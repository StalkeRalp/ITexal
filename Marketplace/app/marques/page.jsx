"use client";import Link from "next/link";import {ArrowUpRight} from "lucide-react";import {useStore} from "@/lib/store";
export default function Page(){const{products}=useStore();const brands=[...new Set(products.map(item=>item.brand))].sort();return <main><section className="page-hero"><div className="container"><p className="eyebrow">Nos marques</p><h1>Des signatures à découvrir.</h1><p>Explorez les maisons présentes dans notre sélection.</p></div></section><section className="section container"><div className="brand-directory">{brands.map((brand,index)=><Link className="brand-directory-card" href={`/catalogue?q=${brand}`} key={brand}><span>{String(index+1).padStart(2,"0")}</span><h3>{brand}</h3><p>{products.filter(item=>item.brand===brand).length} produit(s)</p><ArrowUpRight/></Link>)}</div></section></main>}
